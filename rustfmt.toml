# Matches the Biome settings on the TypeScript side (100 columns), so a file
# does not change shape depending on which language it is in.
#
# `imports_granularity` and `group_imports` would be nice here too, but they are
# nightly-only and this project pins nothing beyond stable.
max_width = 100
use_small_heuristics = "Max"
