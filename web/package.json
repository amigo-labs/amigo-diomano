{
  "name": "diomano-web",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "diomano browser client: Three.js renderer, camera, hand and power menu.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "typecheck": "tsc --noEmit",
    "verify-cross": "node tools/verify-cross.mjs"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^26.1.2",
    "@types/three": "^0.180.0",
    "binaryen": "^123.0.0",
    "playwright": "^1.50.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "wrangler": "^4.118.0"
  },
  "trustedDependencies": [
    "@biomejs/biome"
  ]
}
